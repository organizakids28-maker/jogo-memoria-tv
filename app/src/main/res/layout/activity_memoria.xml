<?xml version="1.0" encoding="utf-8"?>
  <!--
      Layout da MemoriaActivity — usa TableLayout em vez de GridLayout
      para garantir compatibilidade com todas as versões do Android TV.
  -->
  <LinearLayout
      xmlns:android="http://schemas.android.com/apk/res/android"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:orientation="vertical"
      android:padding="16dp"
      android:background="#1A1A2E">

      <LinearLayout
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:orientation="horizontal"
          android:gravity="center_vertical"
          android:paddingBottom="8dp">

          <TextView
              android:layout_width="0dp"
              android:layout_height="wrap_content"
              android:layout_weight="1"
              android:text="Jogo da Memória"
              android:textSize="26sp"
              android:textStyle="bold"
              android:textColor="#E0E0FF" />

          <TextView
              android:id="@+id/tv_placar"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:text="Pares: 0 / 8"
              android:textSize="22sp"
              android:textColor="#A0C4FF"
              android:textStyle="bold" />
      </LinearLayout>

      <TextView
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:text="Use as setas para mover e OK para virar a carta"
          android:textSize="13sp"
          android:textColor="#7070AA"
          android:gravity="center"
          android:paddingBottom="8dp" />

      <!--
          TableLayout 4x4: cada fileira é um TableRow com 4 botões.
          Usa layout_weight para distribuir espaço igualmente.
          Compatível com Android 2.1+ (API 7).
      -->
      <TableLayout
          android:id="@+id/table_cartas"
          android:layout_width="match_parent"
          android:layout_height="0dp"
          android:layout_weight="1"
          android:shrinkColumns="*"
          android:stretchColumns="*" />

  </LinearLayout>
  